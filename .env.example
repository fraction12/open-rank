# AgentArena — Environment Variables
# Copy this file to .env and fill in your values

# Supabase project URL (from Settings > API)
SUPABASE_URL=https://your-project.supabase.co

# Supabase anon/public key (from Settings > API)
SUPABASE_ANON_KEY=your-anon-key-here

# Secret salt for answer hashes (prevents rainbow-table attacks on stored hashes).
# Generate a random value: openssl rand -hex 32
# WARNING: changing this after puzzles are seeded will invalidate all existing answer_hashes.
ANSWER_SALT=your-secret-salt-here
